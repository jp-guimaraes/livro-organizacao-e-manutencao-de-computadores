name: Publicar site e PDFs

# Ativo no repositório público (livro-organizacao-e-manutencao-de-computadores);
# esta cópia em publicacao/ é a fonte de verdade, replicada manualmente para lá.

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "capitulos/**"
      - "mkdocs.yml"
      - "pdf/**"
      - "requirements.txt"
      - ".github/workflows/publish.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalar mkdocs-material
        run: pip install -r requirements.txt

      - name: Instalar TeX Live, pandoc e fontes DejaVu
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            texlive-xetex texlive-latex-extra texlive-latex-recommended \
            texlive-fonts-extra texlive-fonts-recommended \
            texlive-lang-european texlive-lang-portuguese \
            fonts-dejavu pandoc

      - name: Build do PDF (pandoc + xelatex)
        run: python3 pdf/build_pdf.py

      - name: Colocar o PDF em docs/downloads/ (mkdocs copia como arquivo estático)
        run: |
          mkdir -p docs/downloads
          cp livro-organizacao-e-manutencao-de-computadores.pdf docs/downloads/

      - name: Build do site (mkdocs)
        run: mkdocs build --strict

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
